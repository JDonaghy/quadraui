[package]
name = "quadraui"
version = "0.0.1"
edition.workspace = true
description = "Cross-platform UI primitives with native Windows, Linux, macOS, and TUI rendering backends"
license.workspace = true
repository.workspace = true
publish = false

[lib]

[features]
# PTY + vt100 + scrollback engine. No rasteriser — pairs with any backend.
# Enables `quadraui::terminal_engine`. Pulls in portable-pty and vt100.
terminal = ["dep:portable-pty", "dep:vt100"]

# Public TUI rasterisers in `quadraui::tui`. Pulls in ratatui.
tui = ["dep:ratatui", "dep:arboard"]

# Public GTK rasterisers in `quadraui::gtk`. Pulls in gtk4 + pangocairo.
gtk = ["dep:gtk4", "dep:pangocairo", "dep:arboard"]

# Public Win-GUI rasterisers in `quadraui::win`. `windows` is a
# target-specific optional dependency (see the
# `[target.'cfg(target_os = "windows")'.dependencies]` table below),
# same pattern as `macos`'s objc2 family — `dep:windows` unifies fine
# on non-Windows hosts where the dependency simply doesn't exist.
# `#19` landed the Win32 window + Direct2D render-target bootstrap in
# `src/win/run.rs` / `src/win/backend.rs`, gated internally on
# `cfg(target_os = "windows")` so `cargo check --features win` keeps
# type-checking `WinBackend`'s trait-completeness on Linux (the whole
# point of the ubuntu "win compile gate" in ci.yml — see its comment)
# without needing a real WinAPI. Most `Backend::draw_*` methods are
# still `todo!()` stubs — later issues implement each rasteriser.
win = ["dep:windows"]

# Legacy alias kept so `cargo run --example gtk_demo --features gtk-example`
# keeps working as a deprecated spelling. Prefer `--features gtk` going
# forward — both gtk_demo and gtk_app now go through `quadraui::gtk::run`.
gtk-example = ["gtk"]

# Public macOS rasterisers in `quadraui::macos` (AppKit + Core Graphics + Core Text).
# The objc2-family deps are platform-gated below, but that's not the only
# gate: `lib.rs` also cfg's `mod macos` on `target_os = "macos"`, so on a
# non-macOS host `cargo check --features macos` compiles NONE of
# `src/macos/` — it exits 0 having proven nothing, not "type-checked".
# The real gate is `.github/workflows/macos.yml`: a `macos-latest` runner
# that builds and tests this feature for real, triggered by any PR
# touching `quadraui/src/macos/**` (see that workflow's header for why
# it isn't wired to run on Linux/CI at all).
macos = [
    "dep:objc2",
    "dep:objc2-app-kit",
    "dep:objc2-foundation",
    "dep:core-graphics",
    "dep:core-text",
    "dep:core-foundation",
    "dep:arboard",
]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
# Optional — pulled in by the `gtk` / `gtk-example` features.
gtk4 = { version = "0.7", features = ["v4_10"], optional = true }
pangocairo = { version = "0.18", optional = true }
# Optional — pulled in by the `tui` feature.
ratatui = { version = "0.30", optional = true }
# Unconditional (not feature-gated): `StyledText::visible_width` in the
# core `types` module needs real cell-width measurement, not just
# `tui`/`gtk`'s rasterisers — see #471. Kept at the loosest bound
# quadraui itself actually needs ("0.2", i.e. >=0.2.0) rather than
# pinned to whatever patch version happened to resolve at bump time —
# see vendor/vt100-0.16.2-patched/README-PATCH.md (#452) for why a
# tighter bound here would NOT have prevented that conflict anyway
# (vt100's own manifest is the binding constraint, not this line).
unicode-width = "0.2"
# Optional — pulled in by `tui` and `gtk` for system clipboard access.
arboard = { version = "3", optional = true }
# Optional — pulled in by the `terminal` feature.
portable-pty = { version = "0.8", optional = true }
vt100 = { version = "0.16", optional = true }

# macOS-only optional deps. Gated on `target_os = "macos"` so the `macos`
# feature is a no-op on other platforms — keeps cross-platform CI green.
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = { version = "0.5", optional = true }
objc2-app-kit = { version = "0.2", optional = true, features = [
    "NSApplication",
    "NSWindow",
    "NSView",
    "NSResponder",
    "NSRunningApplication",
    "NSGraphicsContext",
    "NSGraphics",
    "NSColor",
    "NSEvent",
    "NSCell",
    "NSMenu",
    "NSMenuItem",
    "NSOpenPanel",
    "NSPanel",
    "NSSavePanel",
] }
objc2-foundation = { version = "0.2", optional = true, features = [
    "NSArray",
    "NSDate",
    "NSGeometry",
    "NSNotification",
    "NSObject",
    "NSProcessInfo",
    "NSString",
    "NSThread",
    "NSTimer",
    "NSURL",
] }
core-graphics = { version = "0.23", optional = true }
core-text = { version = "20.1", optional = true }
core-foundation = { version = "0.9", optional = true }

# Windows-only optional dep. Gated on `target_os = "windows"` so the `win`
# feature is a no-op on other platforms (mirrors the macOS block above).
[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.62", optional = true, features = [
    "Win32_Foundation",
    "Win32_Graphics_Direct2D",
    "Win32_Graphics_Direct2D_Common",
    # DirectWrite text infrastructure (#21): `IDWriteFactory` /
    # `IDWriteTextFormat` / `IDWriteTextLayout` / font-collection metrics
    # lookups in `src/win/text.rs`, plus `ID2D1RenderTarget::DrawText` /
    # `DrawTextLayout` — both cfg-gated inside the `windows` crate itself
    # on this same feature (see that crate's `Direct2D/mod.rs`).
    "Win32_Graphics_DirectWrite",
    "Win32_Graphics_Dxgi_Common",
    "Win32_Graphics_Gdi",
    "Win32_System_LibraryLoader",
    "Win32_UI_HiDpi",
    # `GetKeyState` + `VK_*` — issue #20's per-message modifier reads
    # (`win::run`'s `win_key_modifiers`). Not bundled into
    # `Win32_UI_WindowsAndMessaging`; `WM_*`/`MK_*` live there but the
    # virtual-key API is its own binding-crate feature.
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_WindowsAndMessaging",
] }

[dev-dependencies]
serde_json = "1.0"
# For runnable examples that demonstrate the TUI backend pattern.
crossterm = "0.29"
# For isolated temp-directory creation in compose-controller tests
# that touch the filesystem.
tempfile = "3"

[[example]]
name = "tui_tooltip"
path = "examples/tui_tooltip.rs"
required-features = ["tui"]

[[example]]
name = "gtk_tooltip"
path = "examples/gtk_tooltip.rs"
required-features = ["gtk"]

[[example]]
name = "tui_demo"
path = "examples/tui_demo.rs"
required-features = ["tui"]

[[example]]
name = "tui_app"
path = "examples/tui_app.rs"
required-features = ["tui"]

[[example]]
name = "tui_clipboard"
path = "examples/tui_clipboard.rs"
required-features = ["tui"]

[[example]]
name = "msv_multi_tree"
path = "examples/msv_multi_tree.rs"
required-features = ["tui"]

[[example]]
name = "msv_sc_panel"
path = "examples/msv_sc_panel.rs"
required-features = ["tui"]

[[example]]
name = "gtk_demo"
path = "examples/gtk_demo.rs"
required-features = ["gtk"]

[[example]]
name = "gtk_app"
path = "examples/gtk_app.rs"
required-features = ["gtk"]

[[example]]
name = "gtk_multi_tree"
path = "examples/gtk_multi_tree.rs"
required-features = ["gtk"]

[[example]]
name = "tui_panel"
path = "examples/tui_panel.rs"
required-features = ["tui"]

[[example]]
name = "tui_palette_dual_mode"
path = "examples/tui_palette_dual_mode.rs"
required-features = ["tui"]

[[example]]
name = "tui_selection"
path = "examples/tui_selection.rs"
required-features = ["tui"]

[[example]]
name = "gtk_panel"
path = "examples/gtk_panel.rs"
required-features = ["gtk"]

[[example]]
name = "tui_toast"
path = "examples/tui_toast.rs"
required-features = ["tui"]

[[example]]
name = "gtk_toast"
path = "examples/gtk_toast.rs"
required-features = ["gtk"]

[[example]]
name = "tui_markdown"
path = "examples/tui_markdown.rs"
required-features = ["tui"]

[[example]]
name = "gtk_markdown"
path = "examples/gtk_markdown.rs"
required-features = ["gtk"]

[[example]]
name = "tui_markdown_wrap"
path = "examples/tui_markdown_wrap.rs"
required-features = ["tui"]

[[example]]
name = "tui_indicators"
path = "examples/tui_indicators.rs"
required-features = ["tui"]

[[example]]
name = "gtk_indicators"
path = "examples/gtk_indicators.rs"
required-features = ["gtk"]

[[example]]
name = "tui_search_panel"
path = "examples/tui_search_panel.rs"
required-features = ["tui"]

[[example]]
name = "gtk_search_panel"
path = "examples/gtk_search_panel.rs"
required-features = ["gtk"]

[[example]]
name = "tui_sidebar_search"
path = "examples/tui_sidebar_search.rs"
required-features = ["tui"]

[[example]]
name = "gtk_sidebar_search"
path = "examples/gtk_sidebar_search.rs"
required-features = ["gtk"]

[[example]]
name = "tui_form_groups"
path = "examples/tui_form_groups.rs"
required-features = ["tui"]

[[example]]
name = "gtk_form_groups"
path = "examples/gtk_form_groups.rs"
required-features = ["gtk"]

[[example]]
name = "tui_chart"
path = "examples/tui_chart.rs"
required-features = ["tui"]

[[example]]
name = "gtk_chart"
path = "examples/gtk_chart.rs"
required-features = ["gtk"]

[[example]]
name = "tui_minimap"
path = "examples/tui_minimap.rs"
required-features = ["tui"]

[[example]]
name = "gtk_minimap"
path = "examples/gtk_minimap.rs"
required-features = ["gtk"]

[[example]]
name = "macos_app"
path = "examples/macos_app.rs"
required-features = ["macos"]

[[example]]
name = "macos_demo"
path = "examples/macos_demo.rs"
required-features = ["macos"]

[[example]]
name = "macos_data_table"
path = "examples/macos_data_table.rs"
required-features = ["macos"]

[[example]]
name = "macos_chart"
path = "examples/macos_chart.rs"
required-features = ["macos"]

[[example]]
name = "macos_form_groups"
path = "examples/macos_form_groups.rs"
required-features = ["macos"]

[[example]]
name = "macos_multi_tree"
path = "examples/macos_multi_tree.rs"
required-features = ["macos"]

[[example]]
name = "macos_split"
path = "examples/macos_split.rs"
required-features = ["macos"]

[[example]]
name = "macos_panel"
path = "examples/macos_panel.rs"
required-features = ["macos"]

[[example]]
name = "macos_toast"
path = "examples/macos_toast.rs"
required-features = ["macos"]

[[example]]
name = "macos_indicators"
path = "examples/macos_indicators.rs"
required-features = ["macos"]

[[example]]
name = "tui_data_table"
path = "examples/tui_data_table.rs"
required-features = ["tui"]

[[example]]
name = "gtk_data_table"
path = "examples/gtk_data_table.rs"
required-features = ["gtk"]

[[example]]
name = "tui_form_scroll"
path = "examples/tui_form_scroll.rs"
required-features = ["tui"]

[[example]]
name = "gtk_form_scroll"
path = "examples/gtk_form_scroll.rs"
required-features = ["gtk"]

[[example]]
name = "tui_folder_picker"
path = "examples/tui_folder_picker.rs"
required-features = ["tui"]

[[example]]
name = "gtk_folder_picker"
path = "examples/gtk_folder_picker.rs"
required-features = ["gtk"]

[[example]]
name = "tui_chat"
path = "examples/tui_chat.rs"
required-features = ["tui"]

[[example]]
name = "gtk_chat"
path = "examples/gtk_chat.rs"
required-features = ["gtk"]

[[example]]
name = "tui_hscroll"
path = "examples/tui_hscroll.rs"
required-features = ["tui"]

[[example]]
name = "gtk_hscroll"
path = "examples/gtk_hscroll.rs"
required-features = ["gtk"]

[[example]]
name = "tui_menu_bar"
path = "examples/tui_menu_bar.rs"
required-features = ["tui"]

[[example]]
name = "tui_submenu"
path = "examples/tui_submenu.rs"
required-features = ["tui"]

[[example]]
name = "gtk_menu_bar"
path = "examples/gtk_menu_bar.rs"
required-features = ["gtk"]

[[example]]
name = "tui_split"
path = "examples/tui_split.rs"
required-features = ["tui"]

[[example]]
name = "gtk_split"
path = "examples/gtk_split.rs"
required-features = ["gtk"]

[[example]]
name = "tui_split_tree"
path = "examples/tui_split_tree.rs"
required-features = ["tui"]

[[example]]
name = "gtk_split_tree"
path = "examples/gtk_split_tree.rs"
required-features = ["gtk"]

[[example]]
name = "tui_shell"
path = "examples/tui_shell.rs"
required-features = ["tui"]

[[example]]
name = "gtk_shell"
path = "examples/gtk_shell.rs"
required-features = ["gtk"]

[[example]]
name = "macos_search_panel"
path = "examples/macos_search_panel.rs"
required-features = ["macos"]

[[example]]
name = "macos_sidebar_search"
path = "examples/macos_sidebar_search.rs"
required-features = ["macos"]

[[example]]
name = "macos_menu_bar"
path = "examples/macos_menu_bar.rs"
required-features = ["macos"]

[[example]]
name = "macos_native_menu"
path = "examples/macos_native_menu.rs"
required-features = ["macos"]

[[example]]
name = "macos_right_click_demo"
path = "examples/macos_right_click_demo.rs"
required-features = ["macos"]

[[example]]
name = "macos_hscroll"
path = "examples/macos_hscroll.rs"
required-features = ["macos"]

[[example]]
name = "macos_appshell_demo"
path = "examples/macos_appshell_demo.rs"
required-features = ["macos"]

[[example]]
name = "tui_bottom_panel"
path = "examples/tui_bottom_panel.rs"
required-features = ["tui"]

[[example]]
name = "gtk_bottom_panel"
path = "examples/gtk_bottom_panel.rs"
required-features = ["gtk"]

[[example]]
name = "tui_board"
path = "examples/tui_board.rs"
required-features = ["tui"]

[[example]]
name = "gtk_board"
path = "examples/gtk_board.rs"
required-features = ["gtk"]

[[example]]
name = "tui_diff_view"
path = "examples/tui_diff_view.rs"
required-features = ["tui"]

[[example]]
name = "gtk_diff_view"
path = "examples/gtk_diff_view.rs"
required-features = ["gtk"]

[[example]]
name = "tui_ai_transcript"
path = "examples/tui_ai_transcript.rs"
required-features = ["tui"]

[[example]]
name = "gtk_ai_transcript"
path = "examples/gtk_ai_transcript.rs"
required-features = ["gtk"]

[[example]]
name = "tui_appshell_demo"
path = "examples/tui_appshell_demo.rs"
required-features = ["tui"]

[[example]]
name = "gtk_appshell_demo"
path = "examples/gtk_appshell_demo.rs"
required-features = ["gtk"]

[[example]]
name = "tui_help_layer"
path = "examples/tui_help_layer.rs"
required-features = ["tui"]

[[example]]
name = "gtk_help_layer"
path = "examples/gtk_help_layer.rs"
required-features = ["gtk"]

[[example]]
name = "tui_frame_demo"
path = "examples/tui_frame_demo.rs"
required-features = ["tui"]

[[example]]
name = "gtk_frame_demo"
path = "examples/gtk_frame_demo.rs"
required-features = ["gtk"]

[[example]]
name = "tui_hit_map_recover"
path = "examples/tui_hit_map_recover.rs"
required-features = ["tui"]

[[example]]
name = "gtk_hit_map_recover"
path = "examples/gtk_hit_map_recover.rs"
required-features = ["gtk"]

[[example]]
name = "tui_full_chrome_demo"
path = "examples/tui_full_chrome_demo.rs"
required-features = ["tui"]

[[example]]
name = "gtk_full_chrome_demo"
path = "examples/gtk_full_chrome_demo.rs"
required-features = ["gtk"]

[[example]]
name = "tui_shell_menu"
path = "examples/tui_shell_menu.rs"
required-features = ["tui"]

[[example]]
name = "gtk_shell_menu"
path = "examples/gtk_shell_menu.rs"
required-features = ["gtk"]

[[example]]
name = "tui_pipeline"
path = "examples/tui_pipeline.rs"
required-features = ["tui"]

[[example]]
name = "gtk_pipeline"
path = "examples/gtk_pipeline.rs"
required-features = ["gtk"]

[[example]]
name = "tui_sidebar_panel"
path = "examples/tui_sidebar_panel.rs"
required-features = ["tui"]

[[example]]
name = "gtk_sidebar_panel"
path = "examples/gtk_sidebar_panel.rs"
required-features = ["gtk"]

[[example]]
name = "tui_text_input"
path = "examples/tui_text_input.rs"
required-features = ["tui"]

[[example]]
name = "gtk_text_input"
path = "examples/gtk_text_input.rs"
required-features = ["gtk"]

[[example]]
name = "tui_toolbar"
path = "examples/tui_toolbar.rs"
required-features = ["tui"]

[[example]]
name = "tui_terminal"
path = "examples/tui_terminal.rs"
required-features = ["tui", "terminal"]

[[example]]
name = "gtk_terminal"
path = "examples/gtk_terminal.rs"
required-features = ["gtk", "terminal"]

[[example]]
name = "gtk_toolbar"
path = "examples/gtk_toolbar.rs"
required-features = ["gtk"]

[[example]]
name = "tui_tabgroup"
path = "examples/tui_tabgroup.rs"
required-features = ["tui"]

[[example]]
name = "gtk_tabgroup"
path = "examples/gtk_tabgroup.rs"
required-features = ["gtk"]

[[example]]
name = "tui_activity_nav"
path = "examples/tui_activity_nav.rs"
required-features = ["tui"]

[[example]]
name = "gtk_activity_nav"
path = "examples/gtk_activity_nav.rs"
required-features = ["gtk"]

[[example]]
name = "tui_activity_style"
path = "examples/tui_activity_style.rs"
required-features = ["tui"]

[[example]]
name = "gtk_activity_style"
path = "examples/gtk_activity_style.rs"
required-features = ["gtk"]

[[example]]
name = "tui_dialog_table"
path = "examples/tui_dialog_table.rs"
required-features = ["tui"]

[[example]]
name = "gtk_dialog_table"
path = "examples/gtk_dialog_table.rs"
required-features = ["gtk"]

[[example]]
name = "gtk_editor_font"
path = "examples/gtk_editor_font.rs"
required-features = ["gtk"]

[[example]]
name = "tui_file_dialog"
path = "examples/tui_file_dialog.rs"
required-features = ["tui"]

[[example]]
name = "gtk_file_dialog"
path = "examples/gtk_file_dialog.rs"
required-features = ["gtk"]

[[example]]
name = "tui_modal_occlusion"
path = "examples/tui_modal_occlusion.rs"
required-features = ["tui"]

[[example]]
name = "gtk_modal_occlusion"
path = "examples/gtk_modal_occlusion.rs"
required-features = ["gtk"]

[[example]]
name = "tui_wide_tab_bar_demo"
path = "examples/tui_wide_tab_bar_demo.rs"
required-features = ["tui"]

# #620 — per-tab `TabIcon` glyphs through the `draw_tab_bar_icons`
# sidecar. Paired runners over one shared `AppLogic`, per CLAUDE.md's
# examples-are-paired-by-shape rule.
[[example]]
name = "tui_tab_icons"
path = "examples/tui_tab_icons.rs"
required-features = ["tui"]

[[example]]
name = "gtk_tab_icons"
path = "examples/gtk_tab_icons.rs"
required-features = ["gtk"]

# #631 — active-tab bracket framing via `TabChrome::active_frame`, painted
# through `Backend::draw_tab_bar_with_chrome`. Paired runners over one
# shared `AppLogic`, per CLAUDE.md's examples-are-paired-by-shape rule.
[[example]]
name = "tui_tab_chrome"
path = "examples/tui_tab_chrome.rs"
required-features = ["tui"]

[[example]]
name = "gtk_tab_chrome"
path = "examples/gtk_tab_chrome.rs"
required-features = ["gtk"]

# #595 — `SidebarSystem::reveal`. Both entries are mandatory, not
# cosmetic: without an explicit `[[example]]`, cargo's autodiscovery
# picks the file up with NO `required-features`, so
# `gtk_sidebar_reveal.rs` gets compiled by `cargo test --features tui`
# and fails on `quadraui::gtk` being configured out.
[[example]]
name = "tui_sidebar_reveal"
path = "examples/tui_sidebar_reveal.rs"
required-features = ["tui"]

[[example]]
name = "gtk_sidebar_reveal"
path = "examples/gtk_sidebar_reveal.rs"
required-features = ["gtk"]

# #19 — Win32 + Direct2D window bootstrap smoke test. Windows-only at
# runtime (see the example's module docs), but its `main()` is itself
# `cfg`-split so `cargo build --example win_demo --features win` also
# compiles cleanly on Linux — same "compiles everywhere, only *works*
# on Windows" posture as the rest of `src/win/`.
[[example]]
name = "win_demo"
path = "examples/win_demo.rs"
required-features = ["win"]
